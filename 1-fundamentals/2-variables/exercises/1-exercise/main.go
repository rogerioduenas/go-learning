//Create a variable with an explicit type that stores the name of a city and display the value in the console.

package main

import (
	"fmt"
)

func main() {
	var city string = "Tokyo"
	fmt.Println(city)
}