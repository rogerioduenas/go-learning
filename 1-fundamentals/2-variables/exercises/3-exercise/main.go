// Create a constant with an explicit type that represents the name of a planet and display the value in the console.
package main

import (
	"fmt"
)

func main() {
	const planet = "Earth"
	fmt.Println(planet)
}