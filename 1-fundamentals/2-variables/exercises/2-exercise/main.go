// Create a variable with an inferred type that stores the name of a country and display the value in the console.
package main

import (
	"fmt"
)

func main() {
	country := "Japan"
	fmt.Println(country)
}